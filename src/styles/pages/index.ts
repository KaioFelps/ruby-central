import { styled } from "..";

export const MainContainer = styled("main", {
    width: "calc(100% - 40px)",
    maxWidth: 1264,
    margin: "auto",
})

export const Hero = styled("section", {
    display: "flex",
    flexDirection: "column",
    gap: "1rem",

    marginBottom: "5rem",
})
